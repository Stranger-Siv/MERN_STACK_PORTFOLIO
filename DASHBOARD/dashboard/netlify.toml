# Netlify config for Dashboard app (use when this site’s base = DASHBOARD/dashboard)
# SPA: all routes → index.html for client-side routing

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
