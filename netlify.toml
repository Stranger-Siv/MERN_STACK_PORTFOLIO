# Netlify build config – deploys the PORTFOLIO app (main site, e.g. sivram.in)
# For the Dashboard app instead, set base = "DASHBOARD/dashboard"

[build]
  base = "PORTFOLIO"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
